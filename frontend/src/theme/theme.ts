import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#9381ff",
    },
    secondary: {
      main: "#B8B8FF",
    },
    warning: {
      main: "#FDBB67",
    },
    background: {
      default: "#f5f5f5",
    },
  },
  typography: {
    fontFamily: "Fredoka",
  },
});
