import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#9E74FF",
    },
    secondary: {
      main: "#FF3F6E",
    },
    warning: {
      main: "#FDBB67",
    },
    background: {
      default: "#f5f5f5",
      // default: "#f5f5f5",
    },
  },
  typography:{
    fontFamily: "Fredoka",
  }
});
